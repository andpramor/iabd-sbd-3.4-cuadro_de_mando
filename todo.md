- [ ] En services/extractor.py, la función get_weather_translation(wmo_code) se solapa con la vista datos, la traducción debería ser directa en el servicio en lugar de normalizar el texto y luego traducirlo.

- [ ] Una vez funcione todo con el csv disponible, hacer que funcione el proyecto original y que se maneje todo con información de la db en lugar de un csv intermediario.

